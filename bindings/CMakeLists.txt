# bindings/CMakeLists.txt
#
# Aggregator for language bindings. The Python (pybind11) bindings now live
# in the python/ subdirectory; the WASM (embind) bindings live in wasm/.
add_subdirectory(python)